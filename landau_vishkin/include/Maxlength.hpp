#ifndef MAXLENGTH_HPP
#define MAXLENGTH_HPP

#include <vector>
#include <stdio.h>
#include "EqualityDefinition.hpp"

void maxlength(const std::vector<unsigned char>& R, std::vector<unsigned int>& D, int m, EqualityDefinition& equality);

#endif